apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: devicenator
spec:
  rules:
  - host: devicenator.com
    http:
      paths:
      - path:
        backend:
          serviceName: devicenator
          servicePort: 8080
