apiVersion: v1
kind: Service
metadata:
  name: devicenator-service
spec:
  type: LoadBalancer
  selector:
    component: devicenator
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
status:
  loadBalancer: {}
